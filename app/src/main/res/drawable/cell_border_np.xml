<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Bordure en haut -->
    <item android:top="0dp" android:right="0dp" android:left="0dp" android:bottom="1dp">
        <shape android:shape="rectangle">
            <solid android:color="@color/black"/>
        </shape>
    </item>
    <!-- Bordure en bas -->
    <item android:top="1dp" android:right="1dp" android:left="0dp" android:bottom="0dp">
        <shape android:shape="rectangle">
            <solid android:color="@color/black"/>
        </shape>
    </item>
    <!-- Bordure à gauche -->
    <item android:top="0dp" android:right="1dp" android:left="0dp" android:bottom="0dp">
        <shape android:shape="rectangle">
            <solid android:color="@color/black"/>
        </shape>
    </item>
    <!-- Fond de la cellule -->
    <item>
        <shape android:shape="rectangle">
            <solid android:color="@color/colorEnabled"/>
        </shape>
    </item>
</layer-list>